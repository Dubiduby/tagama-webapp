import "../assets/styles/profile.css";

export default function profile(container) {
  //Dentro de esta función iría todo el contenido de profile
  container.innerHTML = "This is the profile page";
}
