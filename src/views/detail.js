import "../assets/styles/detail.css";

export default function detail(container) {
  //Dentro de esta función iría todo el contenido de detail
  container.innerHTML = "This is the workshop detail page";
}
