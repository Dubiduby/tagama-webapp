import "../assets/styles/workshops.css";

export default function workshops(container) {
  //Dentro de esta función iría todo el contenido de workshops
  container.innerHTML = "This is the My workshops page";
}
